// @flow

import type { ColorMap } from '../types';

const colors: ColorMap = {
  black: '#010101',
  white: '#ffffff',
  whiteOpacity: '#ffffffaa',
  grayLight: '#444444',
  grayDark: '#333333',
};

export default colors;
